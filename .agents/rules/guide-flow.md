---
trigger: always_on
---

- Always use python virtual environment inside etl folder; because the default python version is different and incompatible
- DAG will be run using airflow in wsl2
- airflow virtual environment is in ~/airflow_venv
- airflow installation is in ~/airflow
